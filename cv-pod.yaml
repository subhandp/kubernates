 apiVersion: v1
 kind: Pod
 metadata:
   name: cv-pod
   labels:
     app: web
     name: cv-pod
 spec:
   containers:
     - name: cv-container
       image: subhandp/tailwinddocker:latest
       ports:
         - containerPort: 80
           name: http